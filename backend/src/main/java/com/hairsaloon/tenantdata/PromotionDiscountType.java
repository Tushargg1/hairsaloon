package com.hairsaloon.tenantdata;

public enum PromotionDiscountType {
    PERCENT,
    FIXED,
    /** A bundle price for two or more services, advertised rather than redeemed by code. */
    COMBO
}
